const size = {
  mobile: "334px",
  laptop: "993px",
};

export const device = {
  min_m_view: `(min-width: ${size.mobile})`,
  min_l_view: `(min-width: ${size.laptop})`,
  max_m_view: `(max-width: ${size.mobile})`,
  max_l_view: `(max-width: ${size.laptop})`,
};
